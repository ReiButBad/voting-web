export interface PollItem {
  attachments: string[];
  collectionId: string;
  collectionName: string;
  created: string;
  id: string;
  pickers: any[];
  poll_id: string;
  text: string;
  updated: string;
}